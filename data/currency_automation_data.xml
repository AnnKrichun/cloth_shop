<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- PRIVATBANK AUTOMATED EXCHANGE RATES INTEGRATION SCHEDULED ACTION -->
        <record id="ir_cron_update_privatbank_rates" model="ir.cron">
            <field name="name">Cloth Shop: Auto Update Rates from PrivatBank</field>
            <field name="model_id" ref="base.model_res_currency"/>
            <field name="state">code</field>
            <field name="code">model._update_privatbank_currency_rates()</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(datetime.now() + relativedelta(days=1)).strftime('%Y-%m-%d 01:00:00')"/>
        </record>

    </data>
</odoo>
